#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

class Solution {
public:
    int maxProduct(string s) {
        int ans = 0;
        int len = s.length();
        

        return ans;
    }
};
int main(){
    string s;
    cin >> s;
    Solution sl;
    cout << sl.maxProduct(s) << '\n';
    return 0;
}